python：作用
web  应用  facebook，豆瓣
爬虫程序
科学计算
自动化运维
大数据（数据清洗）
云计算
桌面软件/游戏

python环境解释器
  cpython(官方） 用c语言编写
  pypy（python编写）
  Ironpython（.net编写）
  jypthon(java编写）
  
  python 语法
    默认一行是80个字符，换行是\，严格缩进，变量无需声明可直接赋值，
    标识符规范：
      1，标识符可以含有字母数字下划线，但是不能使用数字开头
      2.标识符不能是python中的关键字和保留字，也不建议使用python的函数名作为标识符，因为这样会到导致函数被覆盖
      3.命名规范：在python中两种命名规范：
          1，下划线命名法   所有字母小写，单次之间使用——分割   max_length
          2，帕斯卡命名法（大驼峰命名法）  首字母大写，每个单次开头字母大写，其余字母小写 MaxLength  HelloWorld
   数值：
      在python中数值分为三种：整数，浮点数（小数），复数  在python中所有整数都是int类型
         在python中整数的大小没有限制，可以是一个无限大的整数，如果数字长度过大，可以使用下划线为分隔符
            10进制数字不能以0开头，其它进制的整数，只要是数字打印就一定是以十进制的形式显示的
            二进制以  0b开头  c = 0b10
            八进制以  0o开头  c = 0o10
            十六进制以 0x开头  c = 0x10
            可以对数字进行运算和赋值
         浮点数（小数） 在python中所有的小数都是float类型
         对浮点数进行运算时，可能会得到一个不精确的结果
    字符串（str) 用来表示一段文本信息，字符串是程序中使用最多的数据类型
              在python中字符串需要使用引号引起来，引号可以是双引号和单引号，相同的引号之间不能进行嵌套，单引号和双引号不能跨行使用
              长字符串，使用三重引号来表示一个长字符串，三重引号可以换行，并且会保留字符串中的格式
              s = '''hello,world'''
         转义字符  可以使用\作为转义字符
                \'表示'    \''表示''   \t表示制表符   \n表示换行符  \\表示\  \uxxxx表示unicode编码 
         格式化字符串   字符串中之间也可以进行加法运算，两个字符串拼接为一个，字符串不能和其它类型进行加法运算
              在创建字符串中可以在字符串中指定占位符
              %s 在字符串中表示任意字符  %3.5s 表示字符串的长度限制在3-5之间   name = '张扬 %s'%'18'
              %f 浮点数占位符
              %d 整数占位符
           格式化字符串中可以直接嵌入变量     c=f'hello {a} {b}'  其中ab为已定义变量
           字符串的复制(将字符串和数字相乘）  *在语言中表示乘法，如果将字符串和数字相乘，则解释器会将字符串重复指定次数并返回
     布尔值（bool）布尔值主要用来做逻辑判断 一共两个True和False  布尔值实际上属于整型，True相当于1，False相当于0
     None(空值） 表示不存在
     类型检查：通过类型检查，可以检查只能值（变量）的类型  type()函数用来检查值的类型，该函数会将检查结果作为返回值返回，可以通过变量接收函数的返回值
     对象： python是一门面向对象的语言  对象就是内存中专门用来存储指定数据的一块区域，对象实际上就是一个容器，专门用来存储数据
          对象结构：每个对象都要保存三种数据   
              -id(标识）:id用来标识对象的唯一性，每个对象都有唯一的id,对象的id就相当于人的身份证号一样，可以通过id（）函数来查看对象的id,
                        id 是由解析器生成的，在Cpython中，id就是对象的内存地址，对象一旦创建，id永远不再该表
              -type（类型）：类型用来标识当前对象所属的类型，比如，int,str,float,bool..,类型决定了对象有哪些功能，
                          python是一们强类型语言，对象一旦创建类型不能修改，通过type()来查看对象类型
              
              -value（值）就是对象中存储的的具体的数据，对于有些对象值是可以改变的，对象分为两大类，可变对象，不可变对象，可变对象的值可以改变
                          之前学习的对象都是不可变对象
      变量和对象：在python中变量相当于对象的别名，变量中存储的不是对象的值，而是对象的id（内存地址）当我们使用变量时，实际上就是在通过id在查找对象
               变量中保存的对象，只有再为变量重新赋值的时候才会改变，变量和变量之间相互独立，修改一个不会影响另一个变量
          类型转换：所谓类型转换，将一个类型的对象转换为其它对象
                  类型转换不是改变对象本身的类型，而是根据当前对象的值创建一个新对象
          类型转换四个函数 int()  float() str() bool()
                调用int()函数不会对原来的变量产生影响，它是对象转换为指定的类型并将其作为返回值返回
                      如果希望修改原来的变量，则需要对变量进行重新赋值
                     规则：布尔值：True ->1 False ->0
                           浮点数：直接取整，省略小数点后的内容
                           字符串：合法的整数字符串，直接转换为对应的数字，如果不是一个合法的整数字符串，则报错
                           对于其它不可转换为整型的会报错
                float()和int()基本一致，不同的是他会将对象转换为浮点数
                str()可以将对象转换为字符串
                bool()可以将对象转换为布尔值，任何对象都可以转换为布尔值，
                    规则：对于所有标识空的对象都会转换为False，其余的转换为True,哪些表示的空：0，None,'' ...
        运算符（操作符）
            运算符可以对一个值和多个值进行运算或各种操作
            算术运算符
              /除法运算符，运算时总会返回一个浮点型
              //整除，指挥保留计算后的整数位，总会返回一个整形
              **幂运算，求一个值的几次幂
              %取模 求两个数相除的余数
             赋值运算符
             关系运算符：在python中，可以对两个字符进行大于等于或小于的运算，当对字符串进行比较时，实际上比较的是字符串的的unicode的编码，比较
                          两个字符串的unicode编码时，是逐位比较的，利用该特性对字符串按照字母顺序进行排序，但对于中文意义不大
                            注意：如果不希望比较两个字符串的Unicode编码，则需要将其转换为数字然后再比较
                ==和！=比较的是值，不比较id
                is和is not 比较是否是同一个对象，比较的是对象的id
            逻辑运算符：
                  not   逻辑非  可以对符号右侧的值进行非运算，对于布尔值，非运算会进行取反操作，对于非布尔值，非运算符会先将其转换为布尔值，然后再取反
                  and  逻辑与  可以对符号两侧的值进行与运算，只有在符号两侧都会True的时候，才会返回True,只要有一个False就返回False
                                与运算是短路与，发现要给False后面就不执行
                  or   逻辑或  短路或，发现一个短路true,后面就不执行
                  逻辑运算符可以连着使用  resutl = 1<2<3   相当于  1<2 and 2<3
               非布尔值的与或运算：
                当我们对非布尔值进行与或运算时，Python会将其当作布尔值运算，最终会返回原值
                与运算规则：
                    与运算是找False，如果第一个值是False,则不看第二个值
                    如果第一个值是False ，则直接返回第一个值，否则返回第二个值
                或运算符规则：
                    或运算符是找True,如果第一个值是True，则不看第二个值
                    如果第一个值是True,则直接返回第一个值，否则返回第二个值
             条件运算符（三元运算符）
               语法：语句1 if 条件表达式 else 语句2
               执行流程：条件运算符在执行时，会先对条件表达式进行求职判断，如果判断条件为True，则执行语句1，返回执行结果
                          如果判断结果为False，则执行语句2，并返回执行结果
                运算符的优先级：
                    和数学中一样，在python运算也有优先级，比如先乘除，后加减
                   非大于and  and 优先级大于or   优先级可以通过括号（）来改变优先级
                
               
                  
        
           
     
             
                
          
      
